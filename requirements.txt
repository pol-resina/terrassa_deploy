# Core dependencies
streamlit>=1.30.0
streamlit-option-menu>=0.3.0
pandas>=1.5.0
numpy>=1.20.0
Pillow>=9.0.0

# Keep compatible werkzeug version (under 3.0) if needed for imported modules
werkzeug>=2.0.0,<3.0.0

# Additional packages
pyxlsb>=1.0.0  # For reading .xlsb Excel files

